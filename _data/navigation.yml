header:
  - titles:
      # @start locale config
      en      : &EN       Archive
      es      : &ES       Archivo
      # @end locale config
    url: /archive.html
  - titles:
      # @start locale config
      en      : &EN       Portfolio
      es      : &ES       Portfolio
      # @end locale config
    url: /pages/portfolio