header:
- titles:
      # @start locale config
      en      : &EN       Posts
      es      : &ES       Posts
      # @end locale config
    url: /pages/posts
  - titles:
      # @start locale config
      en      : &EN       Archive
      es      : &ES       Archivo
      # @end locale config
    url: /archive.html
  - titles:
      # @start locale config
      en      : &EN       Portfolio
      es      : &ES       Portfolio
      # @end locale config
    url: /pages/portfolio
  